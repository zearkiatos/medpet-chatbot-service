{
  "build": {
    "preBuildCommand": "npm install && mkdir -p src/credentials && touch src/credentials/google_service_account.json && echo \"$GOOGLE_SERVICE_ACCOUNT\" > src/credentials/google_service_account.json",
    "dockerfilePath": "Dockerfile",
    "context": "."
  },
  "deploy": {
    "restartPolicyType": "ALWAYS"
  },
  "services": {
    "web": {
      "ports": [80]
    }
  }
}
